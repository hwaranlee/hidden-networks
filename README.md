# hidden-networks

```
$ python main.py --architecture vgg19 --dataset cifar10 --prune-rate 0.999 
  (--init-path: load model before training) (--train-type: [freeze-weight or freeze-score or None])
```
